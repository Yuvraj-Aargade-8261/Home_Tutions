<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.HomeTutions" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
    </style>

    <style name="Theme.HomeTutions" parent="Base.Theme.HomeTutions" />
    
    <!-- Splash screen theme -->
    <style name="Theme.HomeTutions.Splash" parent="Base.Theme.HomeTutions">
        <item name="android:windowBackground">@color/splash_primary</item>
        <item name="android:statusBarColor">@color/splash_primary</item>
        <item name="android:navigationBarColor">@color/splash_primary</item>
        <item name="android:windowLightStatusBar">false</item>
    </style>
</resources>